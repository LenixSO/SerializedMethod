name: CI
on:
  push:
    branches:
      # workflow for this repository, will be feat/* -> develop -> release ( for example )
    - release

permissions: 
  contents: write
  pull-requests: write
  issues: write
    
jobs:
  deploy-upm:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - uses: NoTaskStudios/upmizer@v1
      with:
        package_root: "Assets/MyPackage"
        upm_branch: "master"
        github_token: ${{ secrets.GITHUB_TOKEN }}
